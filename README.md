## In order to build project in Xcode follow these steps:

1) Run `make` command.
2) Run `swift build --generate-xcodeproj`.
3) Make sure correct toolchain is selected in Xcode (`DEVELOPMENT-SNAPSHOT-2016-04-25-a`).